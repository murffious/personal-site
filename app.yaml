# [START app_yaml]
runtime: nodejs10
steps:
  - name: "morg"
    args: ["install"]
  - name: "morf"
    args: ["build:gcp"]

handlers:
  - url: /static
    static_dir: public

  - url: /.*
    script: auto
# [END app_yaml]
